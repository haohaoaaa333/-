
## 2026-07-08 隐藏 VIP 会员区块

用户要求暂时隐藏「我的」页面 VIP 会员订阅功能（升级至 Pro 版 + 月度/季度/年度方案 + 支付按钮）。在 `src/pages/profile/index.tsx` 第 314-348 行用 `{false && <>{...}</>}` 包裹隐藏，代码保留便于后续恢复。

## 2026-07-08 模块"开发中"标注 + 轮播图去VIP

1. **学习页 `src/pages/study/index.tsx`**：资料分析、结构化面试、无领导小组 三个模块标注为"即将上线/开发中"，点击弹出 Toast 提示不跳转。新增 `COMING_SOON_MODULES` Set 控制名单，卡片加 `.subject-card-coming` 半透明样式 + soon-badge 标签。
2. **学习页样式 `src/pages/study/index.scss`**：新增 `.subject-card-coming`、`.subject-icon-coming`、`.subject-soon-tag`、`.soon-badge`、`.subject-coming-msg` 等灰色调样式。
3. **首页轮播图 `src/data.ts`**：CAROUSEL_ITEMS 第一条从"VIP 会员限时开启"改为"历年真题全面上线"，三张轮播图全部去除 VIP/付费相关文案和图片。