# 2026-07-05 工作日志

## PDF 题库解析与导入准备

### 解析完成
- 源目录：`D:\浏览器下载\全国各省34省+国考【历年真-题】\34省考+国考pdf版\国考2000-2025真题pdf\2000-2025国考行测PDF\`
- 解析脚本：`scripts/parse_pdfs_to_json.py`
- 处理了 45 套真题 PDF + 45 套答案 PDF
- 结果：
  - **总计 5,820 题**（包含 584 道资料分析题）
  - **有答案 3,101 题**（53.3%），2007-2020 年份答案覆盖率较高
  - **12 个 batch JSON 文件**，共 8.2MB

### 模块分布（估算）
- 常识判断 (~20题/卷) → mod_common_sense
- 言语理解与表达 (~40题/卷) → mod_language  
- 数量关系 (~10-15题/卷) → mod_quantity
- 判断推理 (~40题/卷) → mod_logic
- 资料分析 (~20题/卷) → mod_data

### 资料分析材料处理
- 云函数 `questions` 已更新 `attachMaterialForDataAnalysis()` 动态匹配材料
- 前端 `src/pages/practice/index.tsx` 已支持显示 material 区域
- 所有题型均包含 `material` 字段

### 导入云函数准备
- 6 个子云函数（importQuestions1-6），每个含 2 个 batch JSON
- 1 个编排器（importQuestions），支持 `import_all`、`stats`、`clear`
- 所有云函数目录均 < 2MB

### 待部署
1. 微信开发者工具中逐一下载并部署 6 个子云函数
2. 部署 `questions` 云函数（含 material 支持）
3. 调用 `importQuestions` → `clear` → 清空旧数据
4. 按序调用 `importQuestions1` 到 `importQuestions6` 导入新数据
5. 重新编译小程序
