# 2026-07-06 工作日志

## 分析 importQuestions stats 结果

- 数据库 5,543 题，JSON 期望 5,820 题，差 277 题
- 按年份定位：2001: -11, 2002: -9, 2004: -7, 2005: -116, 2007: -18, 2019-2025: -116
- mod_unknown 从 384 降到 284（100 题未导入）
- 根因：部分批次 `add()` 操作的大块失败导致 277 题缺失

## 为所有 importQuestionsN 添加 repair action

12 个子云函数 + 编排器全部更新：

- `{ "action": "repair" }`：先查 DB 已有 ID，只导入缺失的题目
- `{ "action": "count" }`：返回批次期望题目数
- 默认（无 action）：全量导入（原有行为）
- 编排器新增 `{ "action": "repair_all" }` 返回修复计划

## 部署步骤（修复模式）

1. 先重新部署 importQuestions + importQuestions1-12（所有含 repair 的版本）
2. 调用每个函数的 `{ "action": "repair" }`（无需 clear，自动跳过已有）
3. 全部完成后调用 `{ "action": "stats" }` 验证是否达到 5,820

## 修复进度

- 2026-07-06 12:14: 已修复到 5,786 / 5,820，仅差 34 题（99.4%）
- 仍缺失分布：2001: -1, 2004: -1, 2005: -2, 2007: -1, 2019-2025: -29
- 模块缺口：mod_unknown: -21, mod_data: -10, mod_language: -1, mod_quantity: -2
## 资料分析材料不全修复

- **根因**：`questions` 云函数的 `.field()` 投影没有包含 `material` 字段，导致 DB 中的材料数据无法返回给前端
- **修复**：`getForPractice`、`getByYear`、`getByPaper` 三处 `.field()` 都加了 `material: true`
- **修复**：`formatQuestion` 材料优先级调整为 `q.material`（DB字段） > `materialMap`（动态匹配） > `content`（自包含题干）
- JSON 中 83.7% 的 mod_data 题目已有 material，修复后这些题目都能正常显示材料

## 重新解析并修复 资料分析材料

- **根因**：parser 把长题干误当作材料，且没正确按 "根据以下资料，回答 X—Y 题" 切分多个材料块
- **修复**：新增 `scripts/fix_materials.py` 后处理脚本，从原始 PDF 重新提取材料文本并按题号范围匹配
- **结果**：
  - 889 道 mod_data 中，623 道（70%）获得完整文本材料
  - 121 道仅有指令行（因原题是图表/图片，无法提取文字）
  - 145 道仍为空（需继续优化）
  - 2015 软件业务收入、工业企业、群众文化机构等文本材料已正确
- **重导入准备**：已重新生成 12 个 batch 文件，重新分发到 importQuestions1-12
